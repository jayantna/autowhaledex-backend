{
  "name": "vault-api-backend",
  "version": "1.0.0",
  "description": "Backend API for vault management",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/",
    "dev": "NODE_ENV=development node server.js",
    "start": "NODE_ENV=production node server.js"
  },
  "dependencies": {
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}